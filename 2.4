import seaborn as sb
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
import math
from pandas.plotting import parallel_coordinates
tetha=0.5;
sk=math.pi
print(sk^2)
def umed(x):
    i=0
    vect=[]
    while (i<len(x)-1):
        j=i+1
        while(j<=len(x)-1):
            vect.append((x[i]+x[j])/2)
            j+=1
        i+=1
    return np.median(vect)

n=[2,5,10,20,50,100]
s=1000
num=0
plt.figure(figsize=(20,20))
for i in n:
    num+=1
    res1=[]
    res2=[]
    for j in range(s) :
        x=[]
        for k in range(i):
            x.append(np.random.standard_cauchy(1)+tetha)
        res1.append(np.median(x))
        res2.append(umed(x))
    plt.subplot(3,3,num)
    l=0
    eq=0
    while(l<1000):
        b=math.fabs(res1[l]-tetha)
        a=math.fabs(res2[l]-tetha)
        if(b < a):
            eq+=1
        elif(b > a):
            eq-=1
        l+=1
    if(eq==0):
        print('eq')
    elif(eq>0):
        print('M')
    else:
        print('M1')
    sb.distplot(res2)
    sb.distplot(res1)
